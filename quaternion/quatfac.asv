[scale aixs]=
%% factotization the quaternions, calculate aixs and angle

N=length(Quad15);
scale = Quad15(:,4);
aixs = Quad15(:,1:3)./[sqrt(ones(N,1)-scale.^2) sqrt(ones(N,1)-scale.^2) sqrt(ones(N,1)-scale.^2)];